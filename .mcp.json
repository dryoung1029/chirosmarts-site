{
  "//": "Moot MCP. Token read from the MOOT_TOKEN env var — safe to commit. Use Tutor's own token.",
  "mcpServers": {
    "moot": {
      "type": "http",
      "url": "https://moot.fly.dev/mcp",
      "headers": { "Authorization": "Bearer ${MOOT_TOKEN}" }
    }
  }
}
