---
/**
 * ChiroSmarts wordmark — vector recreation of the brand logo: a geometric
 * "CHIRO" whose O is a lightbulb, with "smarts" beneath in a lighter tone.
 *
 * Uses `currentColor` throughout so it adapts to light/dark surfaces (the site
 * is dark-themed; the certificate is white). The geometric font falls back
 * across Century Gothic → Questrial (loaded in the page <head>) → Jost.
 *
 * When the final raster/vector art is committed, swap this component's body for
 * an <img>/inline-SVG of the real file — nothing else references the artwork.
 */
interface Props {
  height?: number; // px height of the full mark
}
const { height = 40 } = Astro.props;
const f1 = Math.round(height * 0.6); // CHIR
const f2 = Math.round(height * 0.34); // smarts
const bulbW = Math.round(height * 0.62);
const geometric =
  "'Century Gothic','Questrial','Jost','Twentieth Century',system-ui,sans-serif";
---

<span
  class="cs-logo"
  role="img"
  aria-label="ChiroSmarts"
  style={`display:inline-flex;align-items:stretch;height:${height}px;color:currentColor;font-family:${geometric}`}
>
  <span
    style={`display:flex;flex-direction:column;justify-content:space-between;height:100%`}
  >
    <span style={`font-size:${f1}px;line-height:0.9;letter-spacing:0.06em;font-weight:500`}>CHIR</span>
    <span style={`font-size:${f2}px;line-height:0.9;letter-spacing:0.05em;opacity:0.5`}>smarts</span>
  </span>
  <svg
    width={bulbW}
    height={height}
    viewBox="0 0 64 104"
    fill="none"
    aria-hidden="true"
    style="flex:none;overflow:visible"
  >
    <!-- glass globe = the "O" -->
    <circle
      cx="32"
      cy="31"
      r="27"
      stroke="currentColor"
      stroke-width="4.5"></circle>
    <!-- screw base / cap, aligned beside "smarts" -->
    <line x1="20" y1="66" x2="44" y2="66" stroke="currentColor" stroke-width="4.5" stroke-linecap="round"></line>
    <line x1="22" y1="76" x2="42" y2="76" stroke="currentColor" stroke-width="4.5" stroke-linecap="round"></line>
    <path d="M25 86 q7 11 14 0" stroke="currentColor" stroke-width="4.5" stroke-linecap="round"></path>
  </svg>
</span>
