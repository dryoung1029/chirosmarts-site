{
  "name": "chirosmarts-site",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --port 4322",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "deploy": "astro build && wrangler pages deploy ./dist",
    "astro": "astro",
    "typecheck": "astro check",
    "test": "vitest run",
    "test:watch": "vitest",
    "stream:upload": "node --experimental-strip-types scripts/upload-to-stream.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply chirosmarts --local",
    "db:migrate:remote": "wrangler d1 migrations apply chirosmarts --remote",
    "db:seed:local": "wrangler d1 execute chirosmarts --local --file=./scripts/seed.sql",
    "db:seed:remote": "wrangler d1 execute chirosmarts --remote --file=./scripts/seed.sql"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.0",
    "astro": "^5.13.0",
    "drizzle-orm": "^0.44.2",
    "pdf-lib": "^1.17.1",
    "qrcode": "^1.5.4",
    "stripe": "^22.2.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@cloudflare/workers-types": "^4.20250620.0",
    "@types/qrcode": "^1.5.6",
    "drizzle-kit": "^0.31.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.20.0"
  }
}
